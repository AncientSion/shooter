[gd_scene load_steps=14 format=2]

[ext_resource path="res://textures/shield_circle_w2.png" type="Texture" id=1]
[ext_resource path="res://scripts/Weapon_Shield_Omni.gd" type="Script" id=2]
[ext_resource path="res://scenes/Weapon_Base.tscn" type="PackedScene" id=3]
[ext_resource path="res://scenes/Shaders/Weapon_Shield_Omni_2.gdshader" type="Shader" id=4]
[ext_resource path="res://textures/shield_circle.png" type="Texture" id=5]

[sub_resource type="Gradient" id=2]
offsets = PoolRealArray( 0.2, 0.5, 0.8, 1 )
colors = PoolColorArray( 0.243137, 0.568627, 1, 0, 0.960784, 0.976471, 1, 1, 0.243137, 0.568627, 1, 0, 1, 1, 1, 0 )

[sub_resource type="GradientTexture2D" id=3]
gradient = SubResource( 2 )
width = 40
height = 82
fill_from = Vector2( 1, 0 )
fill_to = Vector2( 0, 0 )

[sub_resource type="Shader" id=4]
code = "shader_type canvas_item;
render_mode blend_mix;

//uniform vec4 arc_color = vec4(0.3, 0.15, 0.15, 1.0);
uniform vec4 arc_color = vec4(0.0, 1.0, 1.0, 0.8);
//uniform vec4 edge_color = vec4(1.0, 0.5, 0.5, 1.0);
uniform vec4 edge_color = vec4(1.0, 1.0, 1.0, 1.0);
uniform float arc: hint_range(0.0, 6.28318530718) = 6; // arc angle in radians
uniform float arc_smooth: hint_range(0.0, 6.28318530718) = .25; // how much of the arc is faded out at edge
uniform float solid_distance: hint_range(0.0, 1.0) = 0.1; // solid outer edge distance

void vertex() {
    UV = (UV - 0.5) * 2.0; // remap so 0.0 is center of the quad
}

void fragment() {
    float r = sqrt(UV.x * UV.x + UV.y * UV.y);
    if (r > 1.0) { discard; }
    float arc2 = arc * 0.5;
    float angle = atan(UV.y, UV.x);
    if (abs(angle) > arc2 + arc_smooth) { discard; }
    float arc_side_factor = min(1.0, 1.0 - (abs(angle) - arc2 + arc_smooth) / arc_smooth);
    if (r > 1.0 - solid_distance) {
        COLOR = vec4(edge_color.rgb, edge_color.a * r * arc_side_factor);
    } else {
        COLOR = vec4(arc_color.rgb, arc_color.a * r * arc_side_factor);
    }
}"

[sub_resource type="ShaderMaterial" id=5]
shader = SubResource( 4 )
shader_param/arc_color = Plane( 0, 1, 1, 0.8 )
shader_param/edge_color = Plane( 1, 1, 1, 1 )
shader_param/arc = 2.0
shader_param/arc_smooth = 0.25
shader_param/solid_distance = 0.1

[sub_resource type="Shader" id=9]
code = "shader_type canvas_item;

uniform float shield_strength : hint_range(0.0,1.0);
uniform float energy_speed = 1.5;
uniform float energy_scale = 8.0;

void fragment() {
    vec2 uv = UV;
    vec4 tex = texture(TEXTURE, uv);

    // --- Color shift (faster toward red) ---
    vec3 strong_color = vec3(0.2, 0.6, 1.0);
    vec3 weak_color   = vec3(1.0, 0.2, 0.2);

    float adjusted_strength = pow(shield_strength, 1.15);
    vec3 shield_color = mix(weak_color, strong_color, adjusted_strength);

    // --- Energy animation ---
    float energy = sin((uv.x + TIME * energy_speed) * energy_scale) * 0.5 + 0.5;

    // --- Flicker (stronger when weak) ---
    float flicker_strength = pow(1.0 - shield_strength, 3.0);

    float flicker = sin(TIME * 40.0) * 0.5 +
                    sin(TIME * 73.0) * 0.3 +
                    sin(TIME * 120.0) * 0.2;
//
    flicker = step(0.6, fract(flicker));
    float instability = flicker * flicker_strength * 1.;
	//instability = 0.0;

    // Optional: push color toward red during flicker
    vec3 flicker_color = mix(shield_color, weak_color, flicker * flicker_strength);

    // --- Final color ---
    vec3 final_color = flicker_color * (1.0 + energy * 0.4 + instability);

    COLOR = vec4(final_color, tex.a * COLOR.a);
}"

[sub_resource type="ShaderMaterial" id=10]
resource_local_to_scene = true
shader = SubResource( 9 )
shader_param/shield_strength = 1.0
shader_param/energy_speed = 1.5
shader_param/energy_scale = 4.0

[sub_resource type="ShaderMaterial" id=8]
shader = ExtResource( 4 )
shader_param/shield_strength = 0.0
shader_param/time = null

[sub_resource type="CircleShape2D" id=6]
resource_local_to_scene = true
radius = 60.0

[node name="Weapon" instance=ExtResource( 3 )]
script = ExtResource( 2 )

[node name="TextureRect" type="TextureRect" parent="." index="2"]
visible = false
margin_left = 46.0
margin_top = -41.0
margin_right = 86.0
margin_bottom = 41.0
texture = SubResource( 3 )

[node name="ColorRect" type="ColorRect" parent="TextureRect" index="0"]
material = SubResource( 5 )
margin_left = -10.0
margin_top = -4.0
margin_right = 30.0
margin_bottom = 86.0
rect_pivot_offset = Vector2( 20, 45 )

[node name="Shield" type="Sprite" parent="." index="3"]
self_modulate = Color( 1.3, 1.3, 1.3, 1 )
material = SubResource( 10 )
texture = ExtResource( 5 )

[node name="Shield2" type="Sprite" parent="." index="4"]
visible = false
material = SubResource( 8 )
texture = ExtResource( 1 )

[node name="Sprites" parent="." index="6"]
visible = false

[node name="Muzzle" parent="." index="7"]
visible = false

[node name="ColNodes" type="Position2D" parent="." index="10"]

[node name="Shield" type="Area2D" parent="ColNodes" index="0"]
collision_layer = 0
collision_mask = 0

[node name="A" type="CollisionShape2D" parent="ColNodes/Shield" index="0"]
shape = SubResource( 6 )

[node name="ShieldSupercharge" type="Timer" parent="TimerNodes" index="0"]
one_shot = true

[node name="ShieldRegen" type="Timer" parent="TimerNodes" index="1"]
one_shot = true

[node name="ShieldBreak" type="Timer" parent="TimerNodes" index="2"]

[node name="ShieldBreakUpdateTimer" type="Timer" parent="TimerNodes" index="3"]
wait_time = 0.5
one_shot = true
